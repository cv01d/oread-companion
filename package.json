{
  "name": "oread-companion",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "license": "UNLICENSED",
  "author": "Claudette Raynor",
  "description": "Roleplay-only React GUI for the oread-cli backend. Start oread-cli with `--api --no-repl` (port 3002), then run `npm run dev`.",
  "keywords": [
    "roleplay",
    "ai",
    "llm",
    "ollama",
    "oread"
  ],
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "test": "npm --prefix client test",
    "install:client": "npm --prefix client install"
  }
}
